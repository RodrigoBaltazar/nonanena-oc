version: '3.8'

services:
  app:
    build: .
    container_name: produtos-app
    ports:
      - "8080:80"  # Mude para 8080 para evitar conflitos
    volumes:
      # Persistir banco de dados SQLite
      - ./data:/var/www/html/data
      # Persistir uploads (se houver)
      - ./uploads:/var/www/html/uploads
      # Montar diretório atual para acessar .env
      - .:/host:ro
    environment:
      - APACHE_DOCUMENT_ROOT=/var/www/html
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Opcional: Nginx como proxy reverso (descomente se necessário)
  # nginx:
  #   image: nginx:alpine
  #   container_name: produtos-nginx
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf
  #   depends_on:
  #     - app
  #   restart: unless-stopped
